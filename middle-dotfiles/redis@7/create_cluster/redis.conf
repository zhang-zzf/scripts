port 6379
cluster-enabled yes
cluster-config-file nodes.conf
cluster-node-timeout 5000
appendonly yes

# no persist
# appendonly no
# save ""

dir .
pidfile ./redis_server.pid
logfile ./redis_server.log

daemonize yes

protected-mode no
